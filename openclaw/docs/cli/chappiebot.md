---
summary: "CLI reference for `chappie chappiebot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `chappie chappiebot ...`
  - You need migration guidance to current commands
title: "chappiebot"
---

# `chappie chappiebot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `chappie chappiebot qr` (same behavior as [`chappie qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `chappie chappiebot qr` -> `chappie qr`

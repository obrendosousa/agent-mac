apiVersion: v1
kind: ConfigMap
metadata:
  name: chappie-config
  labels:
    app: chappie
data:
  chappie.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.chappie/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Chappie Assistant",
            "workspace": "~/.chappie/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Chappie Assistant

    You are a helpful AI assistant running in Kubernetes.
